# =============================================================================
# Financial_RAG — dependency specification
# -----------------------------------------------------------------------------
# Python   : 3.12.3
# Platform : Linux (WSL ext4, native)  ~/Financial_RAG/Financial_env
# Source   : generated from the active Financial_env pip-freeze snapshot
#            (2026-08-08) — every pin below is verified installed & working
#            together (full regression: 411/411 passing).
#
# Install / verify:
#   ~/Financial_RAG/Financial_env/bin/python -m pip install -r requirements.txt
#
# CUDA note: torch is pinned to the PyTorch cu130 wheel. The plain PyPI
# torch==2.13.0 bundles a cu13 runtime that SIGBUS-crashes on this WSL kernel
# (RTX 4050, nvidia-smi 595.79) — keep the +cu130 pin and the index below.
# =============================================================================
--extra-index-url https://download.pytorch.org/whl/cu130

# ---------------------------------------------------------------------------
# Core & Utilities
# ---------------------------------------------------------------------------
appdirs==1.4.4
beautifulsoup4==4.15.0
diskcache==5.6.3
html5lib==1.1
lxml==6.1.1
nest-asyncio==1.6.0
orjson==3.11.9
ormsgpack==1.12.2
pandas==2.3.3
pydantic==2.13.4
pydantic-settings==2.14.2
python-dateutil==2.9.0.post0
python-dotenv==1.2.2
PyYAML==6.0.3
rich==14.3.4
tabulate==0.10.0
tenacity==9.1.4
tiktoken==0.13.0
tqdm==4.70.0
typer==0.27.1
typing_extensions==4.16.0

# ---------------------------------------------------------------------------
# Databases, Vector Stores & Retrieval
# ---------------------------------------------------------------------------
dnspython==2.8.0
pymongo==4.17.0
qdrant-client==1.18.0
rank-bm25==0.2.2
redis==5.3.1

# ---------------------------------------------------------------------------
# Embeddings & Reranking (ML Stack)
# ---------------------------------------------------------------------------
huggingface_hub==1.26.0
nltk==3.10.1
numpy==2.5.1
onnxruntime==1.28.0
Pillow==12.3.0
safetensors==0.8.0
scikit-learn==1.9.0
scipy==1.18.0
sentence-transformers==5.6.1
tokenizers==0.22.2
torch==2.13.0+cu130 ; sys_platform == "linux"
torch==2.13.0 ; sys_platform != "linux"
transformers==5.14.1

# ---------------------------------------------------------------------------
# LLM & Orchestration (LangChain + Groq)
# ---------------------------------------------------------------------------
groq==0.37.1
langchain==1.3.14
langchain-classic==1.0.8
langchain-community==0.4.2
langchain-core==1.5.3
langchain-groq==1.1.3
langchain-openai==1.4.1
langchain-text-splitters==1.1.2
langgraph==1.2.10
langgraph-checkpoint==4.1.1
langsmith==0.10.15
openai==2.53.0
requests==2.34.2

# ---------------------------------------------------------------------------
# Document Parsing (Uploads: PDF / DOCX / HTML / TXT / SGML)
# ---------------------------------------------------------------------------
llama-cloud==0.1.46
llama-parse==0.6.94
pypdf==6.14.2
python-docx==1.2.0

# ---------------------------------------------------------------------------
# Evaluation & MLOps (Module 6: RAGAS + MLflow quality gate)
# ---------------------------------------------------------------------------
alembic==1.18.5
databricks-sdk==0.123.0
datasets==5.0.1
fsspec==2026.6.0
google-auth==2.56.2
instructor==1.15.4
Jinja2==3.1.6
mlflow==3.15.1
networkx==3.6.1
pyarrow==24.0.0
ragas==0.4.3
RapidFuzz==3.14.5
scikit-network==0.33.5
skops==0.14.0
SQLAlchemy==2.0.51
sqlparse==0.5.5

# ---------------------------------------------------------------------------
# Web & API (FastAPI + Streamlit + Arq worker)
# ---------------------------------------------------------------------------
arq==0.28.0
fastapi==0.141.1
gunicorn==26.0.0
hiredis==3.4.1
httpx==0.28.1
httpx-sse==0.4.3
limits==5.8.0
python-multipart==0.0.32
# --- AuthN/AuthZ (Module 7): JWT + password hashing + email validation ---
bcrypt==5.0.0
email-validator==2.3.0
PyJWT==2.13.0
pwdlib==0.3.1
slowapi==0.1.10
sse-starlette==3.4.6
starlette==1.3.1
streamlit==1.60.0
uvicorn==0.52.1

# ---------------------------------------------------------------------------
# Testing & QA
# ---------------------------------------------------------------------------
fpdf2==2.8.7
pytest==9.1.1
pytest-asyncio==1.4.0
